name: Schedule cron

on:
  schedule:
    - cron: "00 02 * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X POST https://bodhi-balance-back.onrender.com/cron/schedule \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
